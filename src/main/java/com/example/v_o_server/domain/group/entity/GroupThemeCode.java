package com.example.v_o_server.domain.group.entity;

/**
 * 그룹 테마 코드. DB의 {@code group_themes.code}(문자열)와 대응하는 표준 값 집합.
 */
public enum GroupThemeCode {
    FRIEND,
    COUPLE,
    FAMILY,
    RANDOM
}
